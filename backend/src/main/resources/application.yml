spring:
  application:
    name: weekend-go-backend
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

weekend-go:
  amap:
    api-key: ${AMAP_API_KEY:}
    base-url: https://restapi.amap.com
